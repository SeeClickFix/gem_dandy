#!/usr/bin/env node

const fs = require('fs');
const lockfile = require('@yarnpkg/lockfile');

let file = fs.readFileSync(process.argv[2], 'utf8');
let json = lockfile.parse(file);

console.log(JSON.stringify(json.object));
